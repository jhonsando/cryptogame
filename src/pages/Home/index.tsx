import { Heading } from '@aws-amplify/ui-react';

const Home = ():JSX.Element => {
  return (
    <Heading level={3}>
      Please use the buttons at the top to test out protected routes! HOMW
    </Heading>
  );
}

export default Home;